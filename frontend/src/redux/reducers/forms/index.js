const initialStore = {
  createProduct: {},
};

export const forms = (store = initialStore, { type, payload }) => {
  switch (type) {
    /* case value:
      return; */
    default:
      return store;
  }
};
